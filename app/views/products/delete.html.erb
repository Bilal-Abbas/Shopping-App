<% @page_title = "Delete Product" %>

<%= link_to("<<< Back to Users", {:controller => 'users', :action => 'index', :class => 'back-link'}) %>

<div class="Product delete">
	<h2>Delete Product </h2>
	<%= form_for(:product, :url => {:action => 'destroy'}, :method => :delete) do |f| %>


		<p> Are you want to delete the <%=  @product.name %> product??? </p>


		<div class="form-buttons">
			<%= submit_tag("Delete Product") %>
		</div>
		<% end %>
	</div>
